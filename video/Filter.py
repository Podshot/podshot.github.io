
UPDATE_URL = "http://podshot.github.io/video/Filter.json"
VERSION = "2.0.0"

inputs = (
    ("Updated filter", "label"),
    )

def perform(level, box, options):
    raise Exception("Filter now at version 2.0!")
